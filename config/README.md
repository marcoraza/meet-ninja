# Config

Configurações versionadas ficam aqui. Secrets não entram neste diretório.

Use `.env.example` como contrato de variáveis locais. Quando houver configurações de roteamento para vaults, repositórios e provedores, criar arquivos exemplo sem dados privados.

Arquivos previstos:

- `vaults.example.json`
- `github.example.json`
- `email.example.json`
